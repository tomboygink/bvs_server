import React from "react";
interface IProps {
}
export declare class Orgs extends React.Component<IProps> {
    constructor(props: any);
    OpenModalRegUser(e: any, tittle: string): Promise<void>;
    render(): React.ReactNode;
}
export {};
